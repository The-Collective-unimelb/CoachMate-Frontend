import classes from "./CoachProfile.module.css";
import Card from "../UI/Card";

function CoachProfile() {
  return (
    <div className={classes["vertical-flex"]}>
      <h1 className={classes["coach-name"]}>NAME</h1>
    </div>
  );
}

export default CoachProfile;
